# tech-challenge-fase2

Boilerplate backend for Tech Challenge Fase 2 (Node.js + TypeScript + Express + Prisma + PostgreSQL).

## Quickstart (local)

1. Copy `.env.example` to `.env` and adjust if needed.

2. Start services with Docker Compose (Postgres):
```bash
docker compose up -d
